// Generated by CoffeeScript 1.9.3
(function() {
  var async, fs, sumFiles, utils;

  fs = require('fs');

  async = require('async');

  utils = require('./utils');

  sumFiles = function(fileNames, callback) {
    return async.map(fileNames, fs.readFile, function(err, result) {
      if (err) {
        return callback(err);
      } else {
        return callback(null, utils.sumStringsAsNumbers(result));
      }
    });
  };

  module.exports = sumFiles;

}).call(this);
